{
  "name": "security-sleuth-smythe",
  "version": "1.0.0",
  "description": "SecSpec Framework for Smythe",
  "scripts": {
    "emoji:install": "cd scripts/emoji-converter && yarn install",
    "emoji:watch": "cd scripts/emoji-converter && yarn watch",
    "dev": "hugo server -D",
    "build": "hugo --minify"
  }
}
